#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;    
int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    
    int n;
    cin >> n;
    for(int i = 0; i < n; i++) {
        string s;
        cin >> s;
        int length = s.length();
        while(true){
            char c;
            c=s[]
        for(int j = 0; j < length; j++) {

            
        }
    }

    
    
    return 0;
}